# TODO

Single tracking file. One flat checklist. Status changes in place by replacing the
marker only — items are never moved or reordered.

`[ ]` Todo · `[o]` In-Progress · `[x]` Done

> **Note on IDs.** CLAUDE.md Module 7 specifies `<PREFIX>-YYYYMMDD-HHMM`. All items
> below were approved and logged in the same minute, so a two-digit sequence suffix
> is appended to keep IDs unique. The timestamp still doubles as the date-added
> field. Flagged rather than silently changed.

Spec for every item below: `docs/specs/FEAT-20260924-1250-lexo-trading-decision-system.md`
Architecture decision: `docs/adr/ADR-20260924-1250-tiered-decision-engine.md`
Discussion history and rationale: `docs/decisions/notepad.md`

- [o] FEAT-20260924-1250-01 — Scaffold the project (uv, FastAPI, pytest) and deploy an empty page to Render before any engine code, so the public URL exists from the start and Friday's deploy is a redeploy rather than a first attempt
- [x] ISS-20260924-1250-02 — Extract the NVDA and AMZN PDF text into `sources/text/` via a committed script, gitignored output, so the transcription reference survives session restarts (it was lost once already) [2026-09-24 13:06]
- [x] FEAT-20260924-1250-03 — Define the fixture schema in Pydantic with fail-fast validation: every claim carries doc, page and verbatim quote, and the app refuses to start if any rule or factor lacks a source [2026-09-24 13:12]
- [x] FEAT-20260924-1250-04 — Transcribe the NVDA fixtures by hand: verdict and stance, execution plan, the three dates, both scenario tables, the decision-driving weighted factors, and the four kill switches — each with page and quote [2026-09-24 14:45]
- [x] FEAT-20260924-1250-05 — Transcribe the thin AMZN fixtures: the three-to-four factors that mirror NVDA's, enough to drive the cross-ticker fan-out demo (depends on FEAT-20260924-1250-03) [2026-09-24 15:35]
- [x] FEAT-20260924-1250-06 — Build the Tier 1 deterministic rule engine: walks a dated event timeline, fires rules, appends to an immutable decision log, and implements the two-consecutive-period confirmation primitive shared by the $195 stop and two kill switches [2026-09-24 15:50]
- [x] FEAT-20260924-1250-07 — Model rule override as a first-class logged event, so the engine can represent the memo's own deliberate override of the Neutral-spring starter-position rule with its stated reason (depends on FEAT-20260924-1250-06) [2026-09-24 16:30]
- [x] FEAT-20260924-1250-08 — Build the Tier 2 factor layer: sector and market factors as shared state that re-rate every affected ticker, emitting proposals only — never auto-executing — with human accept/reject recorded in the decision log (depends on FEAT-20260924-1250-06) [2026-09-24 16:50]
- [x] FEAT-20260924-1250-09 — Author the two divergent futures as event timelines: "Guide Holds" (2026-11-17 FY2028 commentary near 70% with backlog detail) and "Capex Turns" (two of four hyperscalers guiding capex flat or down, 2027-01-26 to 2027-02-06) — both driven by disclosures, not prices [2026-09-24 16:00]
- [x] FEAT-20260924-1250-10 — Prove determinism: canonicalise and hash the decision log, run each scenario twice, assert the hashes match; surface the proof both as a test and as a visible check in the app, because the brief asks us to show that it holds [2026-09-24 16:15]
- [x] FEAT-20260924-1250-11 — Write pytest coverage against the decision logic specifically: trigger boundaries, the two-close confirmation, position-conditional rules, override behaviour and fan-out sign [2026-09-24 17:00]
- [ ] FEAT-20260924-1250-12 — Build the read-only views: what the research concludes, every rule with its source quote and a bidirectional link to the claim, and the honest on-screen factor-coverage disclosure ("N of 28 transcribed", naming what is missing)
- [ ] FEAT-20260924-1250-13 — Build the timeline view: step through dated events, see what the system did and why in plain language at each point, and accept or reject Tier 2 proposals
- [ ] FEAT-20260924-1250-14 — Write the "where this goes next" document: where a learned model would replace the fixed scenario priors, what the source could not supply, and what would be built next — graded as heavily as the code per the kickoff call
- [x] BUG-20260924-1322-15 — Spec defect found during FEAT-20260924-1250-04 transcription: the fan-out table named NVDA #5 Inference Share Loss to Custom Silicon and AMZN #2 Custom Silicon Margin Moat as the headline engine-driven demo, but both sit on the Internal ring and cannot propagate under the spec's own ring rule. Spec §3 corrected — engine fan-out now uses NVDA #22 / AMZN #24 (Market ↔ Market, opposite sign) plus NVDA #11 / AMZN #12 (Sector ↔ Sector, same sign) [2026-09-24 13:22]
- [ ] FEAT-20260924-1322-16 — Surface the custom-silicon asymmetry in the interface as a sourced-but-non-propagating observation: both quotes shown, labelled as a human-observed linkage the engine deliberately declines to act on because both sides are Internal ring, with the reason stated in plain language (arises from BUG-20260924-1322-15; implement within FEAT-20260924-1250-12)
- [x] BUG-20260924-1322-17 — Factor schema from FEAT-20260924-1250-03 stores a single ticker and a single direction per factor, but sector and market factors are shared ticker-agnostic state carrying direction, impact, weight and provenance per ticker. Spec §6.5 now gives the explicit shape. Blocks FEAT-20260924-1250-08 [2026-09-24 14:05]
- [x] BUG-20260924-1421-18 — Zone-to-ring mapping is an unstated assumption that reintroduces BUG-20260924-1322-15: `fixtures/factors.yaml` maps the theme report's composition zones (intrinsic/ecosystem/external, a controllability split over 8 weighted factors) onto Light Cone rings (internal/sector/market, a propagation-scope split over 28 factors). No source states that mapping, and it is demonstrably wrong for `share-ceded-to-custom-silicon` — theme `fc_01 zone external` became `ring: market`, while the same mechanism sits at Light Cone #5 **internal**. Once AMZN exposures land in FEAT-20260924-1250-05 the engine will propagate the custom-silicon pairing that BUG-15 ruled out. Ring must come from the Light Cone diagram only; where a promoted factor has no Light Cone entry, mark the ring unknown and exclude it from propagation rather than inferring it [2026-09-24 15:05]
- [x] BUG-20260924-1421-19 — The memo's Neutral-spring override references `rule_id: platform-neutral-spring-starter-position`, but no rule with that id exists in `fixtures/nvda/rules.yaml`. The override therefore suppresses nothing and the engine cannot demonstrate "rule fired, was overridden, here is why" — the mechanism FEAT-20260924-1250-07 exists to show. Model the platform Neutral-spring starter-position rule explicitly so the override has a target, and add a loader check that every override's rule_id resolves [2026-09-24 15:20]
- [x] BUG-20260924-1421-20 — The three 2026-11-17 commentary rules overlap: `reentry-buy-full-weight` fires at >=70, `bear-row-guidance-below-45` at <45, and `base-row-neither-confirms-nor-breaks` at >=45, so commentary of 72% with backlog detail fires both a buy and a base-row drift on the same date. The disambiguation currently lives in a free-text `window` string the engine cannot read. Give the base-row rule an explicit upper bound, or make rule precedence a declared, tested property [2026-09-24 15:20]
- [x] BUG-20260924-1421-21 — The $195.00 stop carries `consecutive_periods: 2` with no period unit, so on a daily-close timeline two consecutive daily closes would fire it. The memo specifies two consecutive **weekly** closes and states the reason: "The two-close confirmation exists so a single volatile week around an earnings print does not trigger the exit." Add an explicit period unit to the trigger and test the daily-vs-weekly distinction [2026-09-24 15:20]
- [x] ISS-20260924-1421-22 — Three trigger fields carry machine-relevant meaning as free prose and cannot be evaluated: `applies_when: "position != none"` on the stop (position-conditionality is an explicit spec test case), and `window` date ranges on `exit-short-hyperscaler-capex` and `sell-formal-guide-below-45` while rules 1-3 use a structured `event_date` for the same concept. Replace with structured fields before FEAT-20260924-1250-06 reads them [2026-09-24 15:20]
- [x] ISS-20260924-1421-23 — "Backlog detail" is folded into a metric name rather than modelled: `reentry-buy-full-weight` reads `fy2028_commentary_pct_with_backlog_detail` while the other two 2026-11-17 rules read `fy2028_commentary_pct`. Engine behaviour then depends on an event author remembering not to set one of the two keys. Model backlog detail as a separate boolean condition on the trigger [2026-09-24 15:20]
- [x] WISH-20260924-1550-24 — FEAT-20260924-1250-06 says the consecutive-period primitive is shared by the stop and two kill switches, but kill switches were built during FEAT-06 as descriptive/watch items only (their `threshold` field is a compound prose condition, e.g. "below 65% unadjusted or 78% adjusted... with accruals above 20%" — not a single metric/comparator/threshold like Rule.trigger). The primitive itself is generic and reusable (ConsecutivePeriodTracker takes any key), but wiring it to auto-fire the two consecutive-period kill switches against timeline events would need the same threshold-structuring work already done for Rule/Trigger, applied to KillSwitch. Deferred — not part of the two authored scenario timelines (FEAT-09), and kill switches remain correctly sourced/displayed as risk-watch disclosures in the UI either way. Revisit if a scenario ever needs one to fire automatically [2026-09-24 15:50]
- [x] ISS-20260924-1700-25 — Spec §4's Design rationale names the hedging rule (IV rank 0-9 justifies hedging existing exposure through the two windows, but "no strike, expiry or premium is named anywhere here: no options chain was sourced for this workbook") as the one concrete `provenance_status: partial` example, and the requirement-traceability table cites it as satisfying "anything not traceable is flagged in the interface." It was never transcribed into `fixtures/nvda/rules.yaml` during FEAT-20260924-1250-04 — the `partial` path only existed in synthetic tests, not real data, so FEAT-20260924-1250-12's provenance-status rendering had no real `partial` example to show. Found while starting FEAT-12. Transcribe it now, sourced to NVDA-memo p.47 [2026-09-24 17:05]
- [x] ISS-20260924-1710-26 — Spec §9 view 1 asks for "Verdict, stance, both horizons shown separately" but `fixtures/nvda/research.yaml` only carries the tactical stance (Hold/Low/no position) — the 3-5yr intrinsic verdict ("Diamond in Air \| Ocean \| Luminous — N-of-1, 83/100, multibagger gate BORDERLINE") was never transcribed, even though the notepad's Turn 3 quote is right there. Found while building FEAT-20260924-1250-12's research view. Add `verdict` to the Research model and transcribe it, sourced to NVDA-memo p.3 [2026-09-24 17:10]
